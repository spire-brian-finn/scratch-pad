{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "build for pthread test",
            "command": "/usr/bin/clang-10",
            "args": [
                "-fdiagnostics-color=always",
                "-pthread",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}